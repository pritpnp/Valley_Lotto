# Web app (multi-store dashboard). Install on top of requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
PyYAML>=6.0

# Scan capture app (Flask) + production hosting
Flask>=3.0
SQLAlchemy>=2.0
gunicorn>=21.0
psycopg[binary]>=3.1   # Postgres driver for Supabase
