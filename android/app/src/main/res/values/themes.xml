<resources>
    <!-- Brand colors from the logo, so the splash and status bar match the site -->
    <style name="Theme.ValleyLotto" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/brand_green</item>
        <item name="android:windowBackground">@color/paper</item>
    </style>
</resources>
